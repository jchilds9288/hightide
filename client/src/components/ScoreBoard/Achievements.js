import React from "react";
import "./ScoreBoard.css";

export const Achievements = ({ children }) => (
    <div className="achievements">
      {children}
    </div>
  );

export default Achievements;